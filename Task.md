# 任务列表（上游渠道管理）

1. 梳理需求字段与页面布局，确认必展示字段与空态规则。
2. 设计组件结构与状态管理方式，确定数据读取与分页切片逻辑。
3. 实现 `localStorage` 数据读取与初始化回退逻辑（超页回退到最后一页）。
4. 实现表格展示（只读，无操作列），字段映射与空态提示。
5. 实现分页组件与切换逻辑，联动列表数据更新。
6. 自测：空数据、分页切换、刷新后数据仍能正确展示。

